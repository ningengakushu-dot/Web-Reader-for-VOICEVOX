# Web Reader for VOICEVOX [v1.4.4]

> **見ている文字を、好きな声で読み上げる。**
> 選択したテキストだけでなく、画像・PDF・電子書籍の中の文字も読み上げるChrome拡張機能です。
> 音声を作るのはあなたのPCで動くVOICEVOXなので、読み上げる文章が外部へ送られることはありません。

<img width="1280" height="800" alt="Web-Reader-for-VOICEVOX_1280" src="https://github.com/user-attachments/assets/3c9b676e-6856-4a6a-8f44-957c85c2306e" />

[**Chrome ウェブストアでインストール**](https://chromewebstore.google.com/detail/web-reader-for-voicevox/ilcfondcjhaalpcghnhcejioopcbhhla)　|　無料・オープンソース（MIT）　|　Chrome 119以降　|　別途 [VOICEVOX](https://voicevox.hiroshiba.jp/) の起動が必要

## できること

### 選択したテキストを読み上げる

読みたい文字を選んで、右クリックメニューかショートカット（`Alt+Shift+U`）を押すだけです。ページ上に出るアイコンを左クリックしても開始・停止できます。

再生中に別のテキストを選んで読み上げを指示すると、すぐに切り替わります。URLや不要な改行は自動で整形してから読み上げるため、記号の羅列を延々と聞かされることはありません。

### 範囲をドラッグして読み上げる

閲覧中のページの上で、読みたい範囲をドラッグするだけで読み上げます。**ページの表示やサイズは一切変わらない**ので、本文を目で追いながら聞けます。

通常のWebページでは**ページが持っている文字をそのまま読み取る**ため、読み間違いが起きず、待ち時間もほとんどありません（実測: 実サイト8件で読み取れた率 93.7%、待ち時間 54ミリ秒）。

### 画像・PDFの中の文字を読み上げる（β版）

文字として選択できない範囲をドラッグしたときは、自動的に文字認識（OCR）へ切り替わります。画像内の文字、PDF、Kindle等の電子書籍ビューア、コピー禁止サイト、スクリーンショットが対象です。

**縦書きにも対応**しています。選択範囲の組版方向を自動判定するため、縦書きの小説や電子書籍もそのまま読み上げます。PDFビューアなどページ内で範囲選択できない画面では、キャプチャ画像のタブが自動で開き、範囲選択と認識結果の編集ができます。

文字認識は同梱の Tesseract.js（WASM）により**すべてブラウザ内で完結**し、画像が外部へ送信されることはありません。

<details>
<summary><b>β版としての現時点の限界（実文書での計測にもとづく）</b></summary>

文書の種類によっては、読み間違いや読み飛ばしが起こります。

- 枠で囲まれた短いボタンの文字（「OK」など）は、枠を除去しても復元しないことがあります。
- 表の桁区切り（`5,000`）が `5.000` になることがあります。
- 表の列の区切りは読み上げに現れません（行と列の対応は音声からは分かりません）。
- 認識には10〜20秒ほどかかります（小説1ページ相当で実測11〜18秒）。

文字として選択できるページでは、OCRを介さずページ内の文字をそのまま読むため、これらの制約は当てはまりません。

OCR精度に関わる変更は、実画像の変更前後比較を[検証記録](docs/OCR-ACCURACY.md)へ残しています。

</details>

### 声を選び、話し方を調整する

オプション画面から、お使いのVOICEVOXに入っているキャラクターを選べます。速度・音の高さ・抑揚・音量・文の間の長さを、それぞれ好みに合わせて調整できます。

### ページ内アイコンを自分好みに

読み上げの開始・停止に使うアイコンは、**ドラッグで好きな位置へ移動**でき、位置は自動的に保存されます。大きさは16pxから128pxまで選べます（既定は32px）。

見た目は4種類から選べます。**シンプルな円**（既定）／**このアプリのアイコン**／**読み上げキャラクター**／**好きな画像**（PNG・JPEG・WebP・GIF、2MBまで）。「読み上げキャラクター」を選ぶと、いまどの声で読み上げるかがページ上で分かります。

アイコンの**右クリックで画面OCR読み上げをすぐ開始**できます（オプション画面から「設定を開く」動作に変更できます）。

### どんなサイトでも崩さず動く

Chromeの Offscreen API を使っているため、セキュリティ制限（CSP）の厳しい Notion や技術系ブログでも安定して音声を再生できます。ページ内に表示する要素は Shadow DOM で隔離しているので、サイトのレイアウトを崩しません。

## 動作要件（※重要）

本拡張機能は、音声の合成を**あなたのPCで動く VOICEVOX** に任せています。文章が外部のサーバーへ送られないのはこのためですが、その代わり **VOICEVOX を起動しておかないと読み上げられません。**

1. 公式サイト（https://voicevox.hiroshiba.jp/）から VOICEVOX をインストールします。
2. VOICEVOX を起動しておきます（設定は既定のままで構いません）。
3. ブラウザでテキストを選択し、読み上げを開始します。

Chrome 119 以降が必要です。

VOICEVOX アプリを起動せず、同梱の音声合成エンジンだけを常駐させることもできます。手順は「[VOICEVOXアプリを起動せずに使う](#voicevoxアプリを起動せずに使うメモリ節約)」をご覧ください。

## VOICEVOXアプリを起動せずに使う（メモリ節約）

**VOICEVOX のインストールは必要です。**アプリを起動せず、同梱の**音声合成エンジン**だけを常駐させる方法です。声も設定も変わりません。画面には何も出ず、新しくダウンロードするものもありません。

1回読み上げた直後の実測値です。

| 起動方法 | プロセス数 | メモリ使用量 |
| --- | --- | --- |
| VOICEVOX アプリ | 7 | 836 MB |
| エンジンのみ | 1 | 243 MB |

### 設定は最初の1回だけ

次からは、パソコンを起動するだけで使えます。

1. VOICEVOX アプリを閉じます。
2. `Windows` + `R` → `notepad` → 開いたメモ帳に、下の1行を貼り付けます。

   ```
   CreateObject("WScript.Shell").Run """%LOCALAPPDATA%\Programs\VOICEVOX\vv-engine\run.exe""", 0, False
   ```

3. 「**ファイル**」→「**名前を付けて保存**」。保存先は `shell:startup`、種類は「**すべてのファイル**」、名前は `VOICEVOXエンジン.vbs`。
4. `Windows` + `R` → `shell:startup` → 保存したファイルをダブルクリック。
5. 拡張機能のオプション画面を開きます。上の表示が緑になれば成功です（1分ほどかかります）。

<details>
<summary>補足</summary>

- VOICEVOX アプリとエンジンは同じポート（50021）を使うため、同時には起動できません。
- やめるときは、手順4のフォルダーからファイルを削除します。動作中のエンジンを今すぐ止めるには、タスクマネージャーで `run.exe` を終了します。この拡張機能を削除しても、保存したファイルは残ります。
- うまくいかないときは、貼り付けた1行が途中で切れていないか確かめてください。それでも直らないときは、お使いの環境ではこの方法を利用できません。
- 上のパスは、インストーラー版で場所を変えていない場合のものです。ZIP版や場所を変えた場合は、`VOICEVOX.exe` があるフォルダーの中の `vv-engine\run.exe` に書き換えてください。
- 設定はWindowsのユーザーごとです。別のパソコンや別のユーザーでは、同じ手順が必要です。

</details>

## インストール方法

### Chrome ウェブストアからインストール（推奨）

[ストアのページ](https://chromewebstore.google.com/detail/web-reader-for-voicevox/ilcfondcjhaalpcghnhcejioopcbhhla)を開き、「Chrome に追加」をクリックします。

### ソースコードからインストール（開発者向け）

1. このリポジトリをダウンロードまたはクローンします。
2. Chromeで `chrome://extensions/` を開きます。
3. 右上の **「デベロッパーモード」** をONにします。
4. **「パッケージ化されていない拡張機能を読み込む」** を選択し、本プロジェクトのフォルダを指定します。

## プライバシーと安全性
   - 読み上げ対象のテキストデータは、ローカル環境で起動しているVOICEVOXエンジンとの通信に使用されます。
   - 画面OCR読み上げで取得するスクリーンショットは、同梱のTesseract.jsによりブラウザ内でのみ処理され、外部へ送信されることはありません。PDFビューア等でキャプチャタブを開く場合のみ、画像が `chrome.storage.session` に一時保持され、ブラウザ終了時または次回キャプチャ時に破棄されます。
   - 開発者のサーバーを含め、外部サーバーへユーザーデータを送信・保存することはありません。

## 技術仕様

- **Manifest V3 準拠**: セキュリティとパフォーマンスに優れた最新仕様。
- **Service Worker**: バックグラウンドでの安定したAPI通信。
- **Offscreen API**: CSP制限を回避するための独立したオーディオ再生環境。
- **Audio Memory Safety**: 再生済みのBlob URLを即時解放し、ブラウザの負荷を最小限に抑制。
- **Visual Isolation**: Shadow DOM (mode: closed) を使用し、サイト側のCSS干渉を完全に遮断。
- **テキスト優先の段階処理**: 選択範囲をDOMのRangeへ逆引きし、ページ内の文字を直接取得します。表示されていない要素・支援技術向けの隠し要素・他の要素に覆われた文字は読み上げから除外します。取り出せない範囲だけをOCRへ回します。
- **ローカルOCR**: 文字認識にはWASM版Tesseract（Tesseract.js）を拡張機能に同梱。画像・言語データを含め外部への通信は一切行いません。

## 更新情報

### v1.4.4
- 表の罫線やボタンの枠がある画像をOCRすると、行の見出しやボタンの文字が丸ごと読み飛ばされる問題を修正しました。文字の画より長い直線だけを認識前に取り除くようにしています。罫線の無い文書は一切変更していません（既存コーパスの認識結果は変更前後で完全一致。[検証記録](docs/OCR-ACCURACY.md)）。
- ニュース記事・技術文書・表・箇条書き・アプリのUIなど、縦書き小説以外の媒体でもOCR精度を確認・改善しました（[検証記録](docs/OCR-ACCURACY.md)）。
- 画面OCRの待ち時間を短縮しました。文字が十分に鮮明なときは複数倍率での再認識を省略し、実運用でおよそ1/3に短縮しています。認識結果は変更していません。
- VOICEVOXエンジンが一時的でなく繰り返し応答を拒否する状況（話者IDの不一致、他アプリによるポート競合など）で、無意味な再試行を繰り返したり分かりにくいエラーが表示されたりしないよう修正しました。
- 範囲ドラッグ読み上げで、横に並んだメニューやナビゲーションの項目が区切りなく続けて読まれる問題、整形済みテキスト（表形式のテキストなど）で行や列が混ざって読まれる問題を修正しました。
- 括弧が不自然な間を作る問題を修正しました。「彼は「はい」と答えた。」のような文では、括弧の前後で音が途切れ、直後の「と」が切り離されて読まれていました（実測3.27秒→1.92秒）。括弧を外しても語の切れ目が分かるときだけ外し、つながって別の語に聞こえる並びは読点1つにしています。曜日や「（株）」などVOICEVOXが括弧ごと読む表記はそのまま残します。
- 「±」「≒」「〜」「°」など、読み上げると意味が変わってしまう記号を正しく読むようにしました。
- 何も選択せずに読み上げを実行したとき、案内メッセージが表示されるようにしました。
- 精度への寄与が小さいことを実測で確認できた語彙辞書（約1MB）を削除し、パッケージを軽量化しました。認識の仕組みは引き続き画像の証拠のみに基づきます。

### v1.4.3
- 縦書きOCRの精度を改善しました。組版方向を断定できない範囲は縦横の両モデルで認識して比較し、余分に挿入された重複文字の除去、短い列の低確信度漢字の局所再確認を追加しています。実文書コーパスでの比較で悪化ゼロを確認しています（[検証記録](docs/OCR-ACCURACY.md)）。
- 精度を安定して保証できなかった「ルビ（ふりがな）優先読み」設定を削除し、画面OCRを通常の高精度経路へ一本化しました。公開版v1.4.2との実画像比較では、確信度と認識文字列が完全に一致しています。
- ページ内アイコンのサイズ上限を64pxから128pxへ拡張しました。
- OCRの進捗表示が開始直後にほぼ100%で止まって見える問題を修正しました。
- 多様な文章での読み上げの途切れ・不自然さを見直しました。句点の無い長い箇条書きや、短い見出しの直後の長い段落で文の間に無音が空く問題（音声の先読みを次の1件から45秒分へ拡大、長い文は読点で分割）、記号・罫線だけの行や不可視文字による無音・欠落、日付「2026/08/16」・時刻「10:30」・金額「¥12,800」の読み、OCRで崩れたURLの読み飛ばし、英文の折り返しや目次・スペック表の区切りを改善しています。実機のVOICEVOXで39種類の文章を合成して確認しました（[検証記録](docs/RELEASE-1.4.3.md)）。
- 罫線付きの表の画像をOCRすると意味のない文字列になっていた問題を修正しました（横書きで明らかに低品質なときはページ分割を自動判定にして再認識。既存の文書コーパスの認識結果は変更前後で一致。[検証記録](docs/OCR-ACCURACY.md)）。
- 更新案内のレビューリンクを公開済みのストアページへ修正し、文言を中立的な表現に改めました。テキスト未選択時の操作をChromeのエラー一覧に記録しないようにしました。

### v1.4.0
- **ページ内アイコンの見た目を選べるようにしました。** オプション画面の「アイコンの見た目」から次の4種類を選べます。アイコンを大きく表示したときの見栄えを改善するための機能です。
  - **シンプルな円**（既定）: これまでどおりの半透明の円です。
  - **このアプリのアイコン**: 拡張機能のアイコンを表示します。
  - **読み上げキャラクター**: いま設定している読み上げキャラクターを表示します。どのキャラクターで読み上げるかがページ上で確認できます。
  - **好きな画像**: お手持ちの画像（PNG / JPEG / WebP / GIF、2MBまで）を指定できます。画像は128pxの正方形に縮小して保存されます。
- 読み上げ中は緑、エラー時は赤の輪郭でアイコンの状態を示します（画像を選んだ場合）。

> **キャラクター画像について**
> キャラクター画像は拡張機能に同梱しておらず、お使いのPCで起動しているVOICEVOXから取得します。取得した画像がブラウザの外へ送信されることはありません。
> また、キャラクター画像の権利はキャラクターごとの権利者が持ち、アプリ内での表示を一律に許可する規約は存在しません。そのため、規約でアプリ・Webページでの利用が明示的に許可されているキャラクター（東北ずん子・ずんだもんプロジェクトのキャラクター）のみ画像で表示し、それ以外のキャラクターはキャラクター名の頭文字を表示します（機能は同じで、どのキャラクターかは判別できます）。

### v1.3.1
- 読み上げ中にアイコンが途中で待機状態に戻ることがある問題を修正しました（短い文が続く場合）。
- VOICEVOXエンジンが応答しない場合に読み上げが止まったまま操作できなくなる問題を修正し、時間内に応答がなければエラーを表示して復帰するようにしました。
- 拡張機能の更新後に右クリックメニューが表示されなくなることがある問題を修正しました。
- 対応するChromeのバージョン（119以降）を明示しました。

### v1.3.0
- **範囲ドラッグ読み上げ機能を追加しました。** 閲覧中のページ上で範囲をドラッグ選択するだけで読み上げます。通常のWebページではページ内の文字をそのまま読み取るため、読み間違いがなく待ち時間もほとんどありません（実測: 実サイト8件で読み取れた率 61.4%→93.7%、待ち時間 11.3秒→54ミリ秒）。画像内の文字・PDF・Kindle等の電子書籍・コピー禁止サイトなど、テキストとして選択できない文章は、自動的に文字認識（OCR）へ切り替わります。ページの表示・サイズは変わりません。**縦書き・横書きは自動判定**されるため、縦書きの小説にも対応します。ルビ（ふりがな）を優先して読む設定もオプション画面から任意で有効にできます（v1.4.3で削除）。
  - 起動方法: ページ内アイコンの右クリック／右クリックメニュー「画面をキャプチャしてOCR読み上げ」／ショートカット `Alt+Shift+O`／ツールバーの拡張機能アイコンをクリック。
  - ページ内アイコンの右クリック動作は、オプション画面で従来の「オプション画面を開く」に戻せます。
  - PDFビューア等のページ内で範囲選択できない画面では、キャプチャ画像のタブが開き、範囲選択と認識結果の編集ができます。
  - **読み上げの区切り**: 選択肢や箇条書きの記号（`ア` `①` `(1)` など）のあとに短い「間」を入れて読み上げます。設問の選択肢がひと続きに聞こえてしまうのを防ぎます。
  - **認識精度の向上**: 同じ範囲を複数の倍率で認識し、文字位置・一致数・文字単位の信頼度が揃った候補だけを採用します。特定の語句や語彙辞書には依存しません。
  - 文字認識はオープンソースのOCRエンジン「Tesseract」（Tesseract.js / Apache-2.0）を拡張機能に同梱して使用します。処理はすべてお使いのブラウザ内で完結します。

### v1.2.1
- ショートカット（`Alt+Shift+U` / `Shift+Alt+U`）からの読み上げ起動の安定性を改善しました。
- 再生状態の通知を読み上げを要求したタブにのみ送るよう修正し、別タブ・別ウィンドウのアイコン表示が誤って切り替わる問題を解消しました。
- 設定画面で保存済みのキャラクター（先頭キャラクター等）が正しく復元されない問題を修正しました。

## トラブルシューティング

- **ショートカットキーで読み上げが始まらない場合**
  Chromeのアドレスバーに `chrome://extensions/shortcuts` を入力して開き、「Web Reader for VOICEVOX」のショートカット（`Alt+Shift+U` / `Alt+Shift+O`）が割り当てられているかを確認してください。他の拡張機能と競合している場合や未割り当ての場合は、Chromeが許可する範囲で割り当てを見直してください。
- **PDFで右クリックメニューにOCR読み上げが表示されない場合**
  ChromeのPDFビューアでは拡張機能のメニューが表示されないことがあります。その場合はショートカット（`Alt+Shift+O`）またはツールバーの拡張機能アイコンのクリックをご利用ください。
- **ローカルのPDFファイル（file://）でOCR読み上げが起動しない場合**
  `chrome://extensions/` で本拡張機能の詳細を開き、「ファイルのURLへのアクセスを許可する」をONにしてください。
- **OCRの認識精度が低い場合**
  ブラウザのズーム（`Ctrl` + `+`）で文字を大きく表示してから範囲選択すると、認識精度が向上します。文字の端ぎりぎりをなぞるより、少し余白を空けて選択したほうが正確に読み取れます。
- **「VOICEVOXエンジンに接続できません」と表示される場合**
  VOICEVOX が起動しているかを確認してください。起動しているのに接続できないときは、他のアプリがポート 50021 を使っていないかをご確認ください。
- **エンジンだけを起動しようとすると、黒い画面がすぐ閉じる場合**
  VOICEVOX アプリが起動していると、ポート 50021 が使われているためエンジンは起動できません。アプリを閉じてから、もう一度お試しください（[VOICEVOXアプリを起動せずに使う](#voicevoxアプリを起動せずに使うメモリ節約)）。

---

## 利用規約・免責事項

本アプリを使用する前に、必ず以下のドキュメントをご確認ください。
本アプリを使用した時点で、これらに同意したものとみなされます。

- [利用規約](https://github.com/ningengakushu-dot/Web-Reader-for-VOICEVOX/blob/main/docs/TERMS.md)
- [プライバシーポリシー](https://github.com/ningengakushu-dot/Web-Reader-for-VOICEVOX/blob/main/docs/PRIVACY.md)

**【重要：自己責任の原則】**

- 本アプリは個人によって開発されたものであり、無償で提供されています。
- 本アプリの使用によって生じた、いかなる損害（PCの不具合、法的トラブル等）についても開発者は責任を負いません。すべて**利用者の自己責任**においてご利用ください。

**【ライセンス・権利関係】**
- 本リポジトリで開発したソースコードは [MIT License](LICENSE) です。
- 同梱している第三者の著作物（Tesseract.js・日本語学習データ）は Apache License 2.0 です。全文は `LICENSE-APACHE-2.0` にあります。
- 音声の生成には[VOICEVOX](https://voicevox.hiroshiba.jp/)を使用しています。生成された音声の利用（動画への使用など）については、必ずVOICEVOXおよび各ボイスキャラクターの利用規約を遵守してください。
- キャラクター画像は本拡張機能に同梱しておらず、お使いの端末で起動しているVOICEVOXから取得して表示します。キャラクター画像の権利は各キャラクターの権利者に帰属します。ページ内アイコンに画像を表示するのは、各権利者の利用規約を確認し、本アプリの使い方（非商用・無償で配布し、画像は再配布せず利用者の端末内でのみ表示する）が許諾の範囲に入ると確認できた次のキャラクターに限っています。それ以外のキャラクターはキャラクター名で表示します。

  ずんだもん / 四国めたん / 九州そら / 中国うさぎ / 中部つるぎ / あんこもん / 東北ずん子 / 東北イタコ / 東北きりたん / 玄野武宏 / 白上虎太郎 / 青山龍星 / 雀松朱司 / 麒ヶ島宗麟 / 黒沢冴白 / WhiteCUL / No.7 / ぞん子 / 離途 / 冥鳴ひまり / 雨晴はう / 剣崎雌雄 / 猫使アル / 猫使ビィ

  上記のキャラクター画像は、それぞれの権利者の利用規約にもとづき表示しています（VOICEVOX:WhiteCUL、VOICEVOX:離途 ほか、各キャラクターの権利は各権利者に帰属します）。本アプリは無償で提供される非商用のソフトウェアです。
- 文字認識（OCR）には[Tesseract.js](https://github.com/naptha/tesseract.js)（Apache License 2.0）および tessdata_best の日本語学習データを同梱・使用しています。詳細は `vendor/tesseract/LICENSE-NOTICE.md` を参照してください。
- **【重要】** 本ツールで生成した音声を公開する場合、必ず「VOICEVOX:（使用キャラクター名）」のようなクレジット表記が必要です。詳細は[利用規約](https://github.com/ningengakushu-dot/Web-Reader-for-VOICEVOX/blob/main/docs/TERMS.md)をご確認ください。


## 開発支援

このアプリを気に入っていただけた場合は、開発の支援をいただけると嬉しいです。

[![GitHub Sponsors](https://img.shields.io/badge/Sponsor-%E2%9D%A4-ea4aaa?logo=github)](https://github.com/sponsors/ningengakushu-dot)
